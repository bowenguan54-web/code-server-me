.tsbuildinfo
.cache
/out*/
release/
release-packages/
release-gcp/
release-images/
node_modules
/plugins
/lib/coder-cloud-agent
.home
coverage
**/.DS_Store

# Code packages itself here.
/lib/vscode-reh-web-*

# Failed e2e test videos are saved here
test/test-results

# Quilt's internal data.
/.pc
/patches/*.diff~

# AlgoLib generated frontend bundles.
# Source modules live in .run/algo-modules/ and can rebuild these files.
# If deployment cannot run .run/build-algo-lib.sh, remove these rules and commit the bundles instead.
.run/algo-lib-check.js
.run/algo-lib-inline-check.js

# AlgoLib offline vendor dependencies.
# Run scripts/download-vendor-deps.sh on each deployed workspace.
src/browser/static/vendor/
